export interface Product {
  _id: string;
  name: string;
  price: number;
  category: string;
  image: string;
  quantity: number;
  shortDescription: string;
  features: string;
  longDescription: string;
}
